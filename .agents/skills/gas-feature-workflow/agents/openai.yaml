interface:
  display_name: "GAS Feature Workflow"
  short_description: "Add GAS features and RPCs safely"
  default_prompt: "Use $gas-feature-workflow to add a feature or RPC while keeping the GAS contract in sync."
policy:
  allow_implicit_invocation: true
